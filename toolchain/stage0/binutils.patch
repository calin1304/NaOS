diff --git a/bfd/config.bfd b/bfd/config.bfd
index 02a1a685..1e205b49 100644
--- a/bfd/config.bfd
+++ b/bfd/config.bfd
@@ -242,6 +242,10 @@ esac
 
 case "${targ}" in
 # START OF targmatch.h
+  i[3-7]86-*-naos*)
+    targ_defvec=i386_elf32_vec
+    targ_selvecs=
+    ;;
 #ifdef BFD64
   aarch64-*-darwin*)
     targ_defvec=aarch64_mach_o_vec
diff --git a/config.sub b/config.sub
index 3d35cde1..53c9c6b3 100755
--- a/config.sub
+++ b/config.sub
@@ -1970,6 +1970,7 @@ case $os in
 	# Now accept the basic system types.
 	# Each alternative MUST end in a * to match a version number.
 	  abug \
+	| naos* \
 	| aix* \
 	| amdhsa* \
 	| amigados* \
diff --git a/gas/configure.tgt b/gas/configure.tgt
index 7a5645fd..3c6e9924 100644
--- a/gas/configure.tgt
+++ b/gas/configure.tgt
@@ -126,6 +126,7 @@ esac
 generic_target=${cpu_type}-$vendor-$os
 # Note: This table is alpha-sorted, please try to keep it that way.
 case ${generic_target} in
+  i386-*-naos*)    fmt=elf ;;
   aarch64*-*-elf*)			fmt=elf;;
   aarch64*-*-fuchsia*)			fmt=elf;;
   aarch64*-*-haiku*)			fmt=elf em=haiku ;;
diff --git a/ld/Makefile.am b/ld/Makefile.am
index 3ae3d7f1..fb9801d3 100644
--- a/ld/Makefile.am
+++ b/ld/Makefile.am
@@ -153,6 +153,7 @@ endif
 
 # These all start with e so 'make clean' can find them.
 ALL_EMULATION_SOURCES = \
+	eelf_i386_naos.c
 	eaix5ppc.c \
 	eaix5rs6.c \
 	eaixppc.c \
diff --git a/ld/configure.tgt b/ld/configure.tgt
index 60b62e69..eef21d15 100644
--- a/ld/configure.tgt
+++ b/ld/configure.tgt
@@ -82,6 +82,10 @@ fi
 # Please try to keep this table more or less in alphabetic order - it
 # makes it much easier to lookup a specific archictecture.
 case "${targ}" in
+i[3-7]86-*-naos*)
+			targ_emul=elf_i386_naos
+			targ_extra_emuls=elf_i386
+			;;
 aarch64_be-*-elf)	targ_emul=aarch64elfb
 			targ_extra_emuls="aarch64elf aarch64elf32 aarch64elf32b armelfb armelf"
 			;;
diff --git a/ld/emulparams/elf_i386_naos.sh b/ld/emulparams/elf_i386_naos.sh
new file mode 100644
index 00000000..08cd1ff0
--- /dev/null
+++ b/ld/emulparams/elf_i386_naos.sh
@@ -0,0 +1,2 @@
+source_sh ${srcdir}/emulparams/elf_i386.sh
+TEXT_START_ADDR=0x08000000
\ No newline at end of file
